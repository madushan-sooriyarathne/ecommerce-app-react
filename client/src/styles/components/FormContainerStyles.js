import { createUseStyles } from "react-jss";

export default createUseStyles({
  FormContainer: {
    display: "flex",
    flexDirection: "column",
    alignSelf: "stretch",
    minWidth: "45rem",
  },
});
