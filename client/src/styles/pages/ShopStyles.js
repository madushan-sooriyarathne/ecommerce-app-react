import { createUseStyles } from "react-jss";

export default createUseStyles({
  Shop: { width: "100%", padding: "0 10rem" },
  Shop_loadingContainer: {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
  },
});
