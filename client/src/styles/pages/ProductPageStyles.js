import { createUseStyles } from "react-jss";

export default createUseStyles({
  Loading_container: {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
  },
  Error: {
    fontSize: "3rem",
    fontWeight: "300",
    color: "red",
    marginBottom: "5rem",
  },
});
